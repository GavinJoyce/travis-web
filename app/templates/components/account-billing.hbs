{{log account}}
<section class='billing'>
  {{#if isEducation}}
    {{billing-education account=account}}
  {{/if}}

  {{#if isTrial}}
    {{billing-trial account=account plans=plans newSubscription=newSubscription}}
  {{/if}}

  {{#if isManual}}
    {{billing-manual subscription=subscription}}
  {{/if}}

  {{#if isManaged}}
    {{billing-subscription account=account}}
  {{/if}}
</section>

{{#if invoices}}
  {{billing-invoices invoices=invoices}}
{{/if}}